# Definition of Done — Sprint 3

## Frontend

- [ ] Компонент `SearchForm.tsx` создан в `frontend/src/components/`
- [ ] При монтировании загружает маршруты из `GET /api/routes`
- [ ] Дропдаун «Аэропорт» — уникальные аэропорты из routes
- [ ] Дропдаун «Курорт» — фильтруется по выбранному аэропорту
- [ ] Поле «Дата» — `type="date"`, `min` = сегодняшняя дата
- [ ] Поле «Пассажиры» — `type="number"`, min=1, max=8
- [ ] Кнопка «Calculate» disabled, если форма невалидна
- [ ] При сабмите — `POST /api/calculate`, loading state на кнопке
- [ ] При ошибке API — inline сообщение об ошибке под формой
- [ ] При успехе — вызывается `onResult(result)` prop
- [ ] `npm run build` проходит без ошибок

## Тестирование

- [ ] TC-3-01: дропдауны заполняются при открытии, курорты фильтруются по аэропорту
- [ ] TC-3-02: Happy Path — форма заполнена, `onResult` получает данные
- [ ] TC-3-03/04: невалидные данные → кнопка disabled, нет запроса к API
- [ ] TC-3-05: ошибка бэкенда → сообщение под формой, форма не заблокирована

## Деплой

- [ ] Smoke-тест локально: TC-3-02 проходит end-to-end (форма → API → onResult вызван)
